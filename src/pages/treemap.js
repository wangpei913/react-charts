import React, { PureComponent } from 'react'

export default class TreeMap extends PureComponent {
    render() {
        return (
            <div>
                treemap
            </div>
        )
    }
}
