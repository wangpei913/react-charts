import React, { PureComponent } from 'react'

export default class Parallel extends PureComponent {
    render() {
        return (
            <div>
                parallel
            </div>
        )
    }
}
