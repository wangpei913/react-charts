import React, { PureComponent } from 'react'

export default class HeatMap extends PureComponent {
    render() {
        return (
            <div>
                heatmap
            </div>
        )
    }
}
