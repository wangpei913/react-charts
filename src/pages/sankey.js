import React, { PureComponent } from 'react'

export default class Sankey extends PureComponent {
    render() {
        return (
            <div>
                sankey
            </div>
        )
    }
}
